# Assets do Projeto

Esta pasta é destinada a:

- **Screenshots do PDF** gerado (ex.: `screenshot-kpis.png`, `screenshot-graficos.png`)
- **GIF de demonstração** do fluxo: colocar Excel em `input/`, rodar `python main.py`, abrir PDF em `output/`
- **Imagens** para uso no README ou em propostas

Para gerar o PDF de exemplo e capturar telas:

```bash
python main.py
# Abra output/relatorio_*.pdf e faça screenshots das seções
```
